## Packages
input-mask-react | For CPF masking in the input field
lucide-react | Icons for UI elements
framer-motion | For smooth transitions between form steps

## Notes
Static image: /qrcode.png needs to be placed in public folder (handled by backend or user upload)
API: POST /api/consultations for the form submission
Design: Government/Institutional style with specific blue palette
